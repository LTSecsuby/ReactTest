let initialState = {
    footerData: {

    }
};

const footerDataReducer = (state = initialState, action) => {

    switch (action.type) {

        default:
            return state;
    }
};

export default footerDataReducer;